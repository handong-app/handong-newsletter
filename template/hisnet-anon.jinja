<div class="anon-desc">※ 조회수가 가장 높은 게시물 5개입니다.</div>
<div class="feat-list">
  {% for item in anon_hot %}
  <div class="feat-item">
    <a href="{{ item.link }}" class="feat-text-title">{{ item.title }}</a>
    {{" ".join(item.content.split(" ")[:65])}}
  </div>
  {% endfor %}
</div>
